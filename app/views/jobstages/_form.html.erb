<%= form_for(@jobstage) do |f| %>
  <% if @jobstage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobstage.errors.count, "error") %> prohibited this jobstage from being saved:</h2>

      <ul>
      <% @jobstage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
